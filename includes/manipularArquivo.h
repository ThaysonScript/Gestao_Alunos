/*

FILE *fptr
fptr = typeRead(filename, mode);

w - Writes to a file
a - Appends new data to a file
r - Reads from a file

fopen() == abrir arquivo
fclose(fptr) == fechar arquivo

*/

// manipular arquivos
void criarArquivo();
void lerArquivo();
void atualizarArquivo();
void deletarArquivo();
